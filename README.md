# BDS Library

A brutalist/editorial music archive built with Next.js 15, React, TailwindCSS and Framer Motion.

## Development

```bash
npm install
npm run dev
```

Place real audio files in `public/audio` matching the placeholder URLs in `data/tracks.js`.

## Deployment

Deploy directly to Vercel. The app uses the Next.js app router and local metadata only.
